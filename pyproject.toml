[tool.black]
line-length = 88
exclude = '/(sdk|venv|\.git)/'

[tool.isort]
profile = "black"
skip = ["sdk"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]